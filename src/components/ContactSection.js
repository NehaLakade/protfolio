import ContactForm from './ContactForm';

const ContactSection = () => (
  <section id="contact" className="min-h-screen bg-gray-300 p-8 flex flex-col items-center justify-center">
    <ContactForm />
  </section>
);

export default ContactSection;
